cubes:
  - name: region
    sql_table: dim_region

    dimensions:
      - name: region_id
        sql: region_id
        type: number
        primary_key: true

      - name: region
        sql: region_name
        type: string

      - name: country
        sql: country
        type: string
